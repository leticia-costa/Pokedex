import { createGlobalStyle } from "styled-components";

// Reset CSS e estilo global (GLobalStyle)
export const GlobalStyle = createGlobalStyle`
*{  
    box-sizing: border-box;
    margin: 0;
    padding: 0;
    text-decoration: none;
    background: #E5E5E5;
    font-family: 'Nunito', sans-serif;
}
`;